{
  "name": "pondlife",
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "@weborigami/origami": "0.0.51"
  },
  "license": "MIT",
  "scripts": {
    "build": "rm -rf build; ori @copy src/site.ori, @mkdir/build",
    "docs": "ori '@svg docs/diagram.ori, { createLinks: false }' > docs/site.svg",
    "start": "ori @serve @watch src, =@debug site.ori"
  }
}
